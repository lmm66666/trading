# 项目开发入口

## 项目与当前阶段

本项目是 Go 编写的 A 股与大宗商品期货行情、指标、策略扫描与回测平台。当前阶段为版本化策略内核与行情工作台稳定化；方向和进入条件见 [Roadmap](docs/roadmap.md)。系统运行规则见 [系统设计](docs/architecture/system-design.md)，操作流程见 [运行手册](docs/operations.md)。根目录不创建 `README.md`。

## 文档驱动开发

使用本机 `$document-driven-development` skill 的 Change、Bootstrap 或 Audit 模式；通用流程和模板保存在 skill，项目不复制模板库。若 skill 不可用，先报告缺失；仍可进行只读分析，不自行绕过本文件门禁。

本项目唯一文档导航是 [功能目录与源码地图](docs/design/README.md)：

- `docs/architecture/`：跨模块边界、数据流和全局不变量。
- `docs/design/README.md` 面向人的功能导航；`workflows/` 解释业务流程，`strategies/` 解释具体策略，其他设计文档维护技术契约。业务说明使用 `kind: explanation`、`status: baseline-review`、`authority: code-derived` 和完整代码基线，不声明源码归属、不覆盖已批准契约；一个实质源码文件仍恰有一个技术设计 `owns` 所有者。
- `docs/standards/`：[工程标准](docs/standards/engineering.md) 与 [HTTP 契约](docs/standards/http-api.md)。
- `docs/changes/active/<date>-<slug>/`：复杂变更的需求、目标设计和验证证据；验收完成后移入 `archive/`。
- `docs/changes/archive/legacy/`：迁移前历史 REQ 记录，保留原证据与状态，不用于推断当前流程。
- `docs/adr/`：仅在出现值得长期独立保存的跨模块决策时创建；不与变更记录重复。

修改前先从功能导航读相关业务说明，再通过源码地图定位模块，读取模块设计及上游架构；涉及公开 HTTP 行为同时读取 API 设计与 HTTP 契约；复杂变更还需读取活动变更的 requirements/design。历史变更不能代替当前设计。

## 项目审批与冲突门禁

设计定义应有行为，测试提供符合性证据。草稿不覆盖已批准文档；已批准变更仅在批准内容版本及明确范围内覆盖当前设计。目标设计先在开发分支描述合并后状态，经批准后实现；主分支保持已验收设计。

- 代码与设计、需求与设计或其他权威文档不一致时，暂停受影响路径，向用户列出具体差异、保持设计/改变设计两种方式及影响，由用户裁决。
- 不自行认定代码错误或文档过期。已有同一范围的明确裁决直接复用；用户已授权恢复原设计时，不重复审批未变的设计。
- 改变批准语义须先修订受影响的需求、设计和契约，绑定新内容版本重新批准；原测试结果标记为过时并重验。未决事项不阻止无关分析。
- 批准记录必须来自实际用户决定，包含批准范围、内容版本、批准人和时间。迁移不补造历史批准，不把确认存量缺陷存在当成批准维持缺陷。

## 项目变更与评审门禁

- **复杂变更**：新增业务能力/模块；改变公开 API、持久化、策略或引擎语义；调整跨模块边界；涉及迁移/兼容、基础设施/外部依赖/并发模型；或风险不能由局部回归测试证明。先批准需求与目标设计，再写生产实现。
- **轻量缺陷**：仅恢复已批准语义、局限单模块、无新增能力或迁移/兼容变化，并能由明确回归测试证明。先完成必要裁决，复现失败测试后最小修复；设计已有完整规则时不制造无意义文档差异，缺失的边界或验证证据才补充。
- **机械维护**：可证明无设计语义变化，声明理由并运行适用检查；无法证明等价则升级。
- 新业务逻辑和缺陷修复测试先行，遵守 Karpathy 编码原则，显式假设、最小修改，不为假设中的扩展增加机制。
- 需求 → 设计 → 测试 → 代码依次评审；上游未通过不进入下一层。复杂变更完成后必须由独立子 Agent 评审架构、遗留清理、简化和缺陷；其他场景尽量不用子 Agent，确有必要先征得用户同意。
- 变更整体 `status` 只保存在 requirements.md；审批用 `approval_status`，验证用 `result`。审批、状态和归档按 skill 执行；项目历史例外与机器门禁见工程标准。
- 完成时把长期有效规则回填架构/设计/标准，逐项记录真实验收结果；未完成必要门禁不得标记 implemented、归档或合并。

## 工程与安全门禁

所有开发必须遵守 [工程标准](docs/standards/engineering.md)，其中保留日志脱敏、数据库索引/事务、测试覆盖率和交付要求。总覆盖率不低于 80%，核心行情、指标、策略、回测各不低于 90%；不得用 SQL mock 或只编译替代获批远端 MySQL 8.4/x86_64 随机隔离库验收；应用镜像按 linux/amd64 构建。

快速检查：`npm --prefix web run check && go test ./... && go vet ./...`。默认本地门禁：`bash scripts/verify.sh`。按工程标准的风险触发条件追加 `--mysql`、`--image` 或 `--full`；适用门禁缺配置、Docker 或其他前提时准确报告未完成，不得伪报通过。未触发的外部门禁记录为不适用及理由。`config.yaml` 只在本地保存，不提交，不进入镜像。

新代码依赖方向、DDD 边界和已有兼容例外以系统设计和源码地图为准；不得扩大兼容栈。工程标准是详细规则的唯一权威来源，本节只是入口摘要。

## Git 与协作

- 中文沟通；复杂操作先规划，设计前确认足以影响方案的频率、数据量及兼容条件，避免过度设计。
- 默认从 main 建立 `codex/` 分支和独立 worktree；先检查工作区，有远端先尝试同步，失败时明确报告，不覆盖用户修改。
- 一个 commit 负责一件事，message 使用英文。
- 全部验证和评审通过后合回 main，切回 main 并清理已合并分支；不因任务结束而自动推送远端。
- 非必要不调用浏览器，需要使用前先征得用户同意。
