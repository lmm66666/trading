---
kind: explanation
status: baseline-review
authority: code-derived
baseline_revision: 0aadb823d45701cd3c1d702f510bf97420bb4fc1
owns: []
related: []
---

# 日线 B1：为什么这根日线会出现买入信号

日线 B1 寻找“先放量上涨，随后有限度回撤，同时均线保持上升、J 值较低”的形态。当前策略 ID 为 `daily_b1_buy`，版本为 `1`。下面说明当前实现怎样判断，不代表这种形态已经证明有超额收益。

你可以先读 [判断规则](#如何判断信号) 和 [具体例子](#用一个例子检查理解)，再读 [实现与修改影响](#实现与修改影响)。它描述现有代码，仍待你判断业务口径是否合理；扫描窗口、数据版本、任务、接口和结果保存统一见 [策略扫描流程](../workflows/strategy-scan.md)。

## 输入与输出

输入是同一证券、固定数据版本的一组日线和对应复权因子，指标由上游统一计算。每根日线到来时，策略只读当前及历史值，输出“不买入”或“买入”；买入原因记为 `daily_b1_pullback`。本策略没有主动退出动作，也不负责下单或资金管理。

策略声明加载 30 根预热日线，默认持有期为 10 根。预热数据如何进入回放由扫描或回测用例决定；默认持有期供回测使用，不会让扫描自动跟踪买入后的收益。输入数据缺失与指标尚未形成是不同问题，前者可能在进入策略前就被拒绝。

## 如何判断信号

### 先统一计算口径

涨幅、回撤、20 日均线和 KDJ 都使用前复权价格；成交量使用原始成交量。这里的“日”指数据里的日线根数，不是自然日。指标窗口不足时为无效值，不能把它当成零来参与判断。

以下是默认参数。参数名供你在接口或代码中定位，不影响正文阅读。

| 你可以调整什么 | 默认值 | 准确含义 | 参数名 |
|---|---:|---|---|
| 放量倍数 | 2 | 当日成交量 ÷ 含当日在内的最近 20 根日线均量 ≥ 2 | `volume_ratio` |
| 上涨幅度 | 5% | 当日相对上一根日线的前复权收盘涨幅 ≥ 5% | `rally_pct` |
| 最大回撤 | 15% | 相对跟踪中最高收盘价的跌幅 ≤ 15% | `pullback_pct` |
| 最长回撤 | 10 根 | 当前日线与最近一次达到或超过最高收盘价的日线，索引差 ≤ 10 | `pullback_bars` |
| J 值上限 | 40 | 当日 KDJ 的 J 值必须严格小于 40 | `kdj_threshold` |
| 均线上升检查 | 10 次 | 最近 10 次相邻日线的 MA20 比较都必须严格上升，共涉及 11 个均线值 | `ma_trend_lookback` |

20 日均线和 9 日 KDJ 周期在当前策略中固定，不属于上述可调整参数。KDJ 使用最近 9 根前复权高低价和当前收盘价计算 RSV；K、D 初值均为 50，依次按 2/3 旧值加 1/3 新值平滑，`J = 3K − 2D`。最高价等于最低价时 RSV 取 50。完整计算见 [KDJ 实现](../../../internal/indicator/kdj.go)。

### 每来一根日线，依次做什么

1. **等待放量上涨。** 当天同时满足放量倍数和上涨幅度，开始跟踪，以当天收盘价作为初始峰值。当天不发回撤买入信号。
2. **更新上涨过程。** 后续收盘价达到或超过峰值，就更新峰值及其所在日线。这一天仍不发回撤信号；即使只是与峰值持平，也会重新计算回撤时长的起点。
3. **检查回撤是否有效。** 收盘价低于峰值时，计算回撤深度和时长。超过任一上限，整轮候选失效；否则继续检查买入条件。这里不要求先出现反弹，第一根符合条件的下跌日线就可能入选。
4. **检查均线与 J 值。** MA20 连续严格上升，且 J < 40，才输出买入信号 `daily_b1_pullback`。不满足时不发信号，但不会仅因均线或 J 不合格而清除回撤候选。
5. **遇到新的放量上涨，重新开始。** 新的放量上涨会替换尚未结束的候选，不把两轮上涨拼接起来。这个判断优先于峰值和回撤判断。

**同一轮回撤允许连续多天发信号。** 当前实现没有“发出一次信号便结束本轮”的动作；之后的日线只要仍满足条件，就可能再次发信号。信号如何进入候选名单见 [策略扫描流程](../workflows/strategy-scan.md)；回测是否成交由引擎结合持仓决定。

当收盘价、上一收盘价、成交量或均量不可用，或均量不大于零时，这根日线直接不发信号，也不推进回撤跟踪。均线或 J 不可用则在跟踪推进后阻止当日信号。这些是当前代码行为，不应理解成缺失行情一定会被自动补齐。

## 用一个例子检查理解

下面是说明规则的假设输入，省略了此前的指标预热数据。价格均为前复权收盘价；表中明确给出的量比、均线和 J 值视为已计算输入，不声称仅凭这几根价格就能推导它们。

| 日线 | 收盘价与条件 | 系统怎样理解 | 当日买入信号 |
|---|---|---|---|
| D0 | 100 | 尚无候选 | 无 |
| D1 | 106，量比 2.2 | 涨幅 6%，建立放量上涨候选 | 无 |
| D2 | 110，不满足新的放量上涨 | 更新最高收盘价为 110 | 无 |
| D3 | 105，不满足新的放量上涨；MA20 检查通过，J=35 | 从峰值回撤约 4.55%，距峰值 1 根 | 有 |
| D4 | 104，不满足新的放量上涨；MA20 检查通过，J=36 | 回撤约 5.45%，距峰值 2 根；候选仍有效 | 仍然有 |

截止 D4 的扫描返回 D4 的信号，不把 D3、D4 作为两条历史信号都返回。若仅把 D4 的 J 改为 40，则截止 D4 不入选，即使 D3 曾经有信号。

以下边界值得你逐项确认是否符合策略本意。除特别说明外，假设其他必要条件均满足，且当天没有新的放量上涨：

| 边界 | 当前行为 |
|---|---|
| 涨幅恰好 5%，量比恰好 2 | 开始新候选，当天不发回撤信号 |
| 峰值 100，当前收盘 85 | 回撤恰好 15%，深度检查通过 |
| 距峰值恰好 10 根 / 11 根 | 10 根通过时长检查；11 根使候选失效 |
| 当前收盘等于峰值 | 更新峰值日期，不作为回撤 |
| J 恰好为 40，或均线比较中有一次持平 | 不发买入信号 |
| 回撤深度超过 15%，第二天又回到范围内 | 旧候选已失效，不自动恢复，需等待新的放量上涨 |

这些例子是可审阅的行为说明。已有测试覆盖到哪里、哪些边界目前只是静态核对，见文末的验证依据。

## 实现与修改影响

| 想理解或修改什么 | 实现入口 | 影响边界 |
|---|---|---|
| 默认参数、均线和 J 条件 | [daily_b1.go](../../../internal/strategy/builtin/daily_b1.go)：`Definition`、`dailyParameterSpecs`、`OnBar` | 修改默认行为需要新策略版本；单次请求传参数不等于修改默认行为 |
| 回撤开始、失效和重启 | [pullback_tracker.go](../../../internal/strategy/builtin/pullback_tracker.go)：`Advance` | 与底部倍量回撤策略共享，修改时要检查另一策略 |
| 均量是否包含当天 | [sma.go](../../../internal/indicator/sma.go)：`smaSeries` | 共享均值计算，不能只为 B1 静默改变其他使用者 |
| 如何计算复权价格 | [adjustment.go](../../../internal/market/adjustment.go)：`AdjustedPrice` | 当前以原始价格乘当时最近已知因子的分子／分母；固定数据版本决定可见的因子修订 |
| 实例隔离和禁止未来数据 | [策略模块设计](../internal/strategy.md) | 全部策略共用的技术契约，本文不重复定义 |

使用条件：本文按当前日线策略解释，不引入分钟级运行或实时交易需求。业务阈值为何最初选择 2 倍、5%、15% 等数值，现有证据不足以还原研究依据，因此只记录默认值，不编造选择理由。

## 验证依据与待判断事项

| 要证明的行为 | 证据 | 验证边界 |
|---|---|---|
| 先上涨，再有限回撤，才出现候选 | [跟踪器测试](../../../internal/strategy/builtin/pullback_tracker_test.go)：`TestPullbackTrackerSignalsOnlyAfterConfirmedPullback` | 不单独证明均线与 J 的组合 |
| 回撤过深／超时失效，新的上涨可重新启动 | 同文件的 `TestPullbackTrackerExpiresWhenDepthOrDurationIsExceeded`、`TestPullbackTrackerRestartsAfterExpiredWindow` | 不等于覆盖所有等号边界 |
| B1 合成样本发信号，追加未来数据不改变既有信号 | [前缀测试](../../../internal/strategy/builtin/prefix_test.go)：`TestDailyB1PastSignalsDoNotChangeWhenFutureBarsAreAppended` | 合成样本，不代表市场收益验证 |
| 特征、预热和部分默认参数 | [定义测试](../../../internal/strategy/builtin/daily_b1_test.go)：`TestDailyB1DefinitionDeclaresAllForwardFeaturesAndWarmup` | 不是完整策略场景测试 |
| 连续信号、J=40、均线持平、回撤等号与本文 D0–D4 示例 | 上述策略、跟踪器和指标源码 | 静态核对；本文逐行例子未新增为自动化用例 |

**需要你判断的业务选择：** 同一轮回撤是否允许连续发信号？均量是否包含当天？达到峰值但没有创新高时，是否重新计算回撤时长？本次整理不改变这些行为，也不把对文档结构的认可视为对这些选择的批准。

代码基线、实际执行检查与范围见 [本批验证记录](../../changes/archive/2026-09-19-readable-design-pilot/verification.md)。日期和覆盖率集中保存在验证记录，不在多份设计中复制。
