---
result: pending
---

# TV 风格工作台与自选清单验证记录

本变更处于实施阶段，验证未开始。完成后按需求验收表（requirements.md §5）逐项记录真实验证结果；门禁要求：`go test ./... && go vet ./...`、`npm --prefix web run check`、`bash scripts/verify.sh --mysql`（新增表与 SQL，触发 MySQL 门禁）。
