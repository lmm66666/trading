---
result: passed
---

# 回测工作台验证

需求见 [requirements.md](requirements.md)，设计见 [design.md](design.md)。2026-09-21 用户对会话方案回复“嗯 没问题”；设计落实记录 cf6e540，正文绑定sha256（不含frontmatter），实现 88bebf7。当前规范已回填 [前端设计](../../../design/web.md)。

| 需求 | 实现 | 验证 | 结果 |
|---|---|---|---|
| BT-001 | BacktestForm / backtestPreferences | 默认请求不变、29.99%→2999、非法精度/参数/空金额、日期直接输入、证券搜索与手数 | passed |
| BT-002 | BacktestPanel / BacktestReport | 新任务运行/失败保留报告，成功切换资源任务ID；草稿编辑后本机提交条件冻结 | passed |
| BT-003 | App / preferences / 资源组件 | 刷新恢复草稿与上下文；隐藏返回恢复查询、报告和分页；浏览器往返确认 | passed |
| BT-004 | useRunPolling / BacktestPanel | 恢复无空态闪烁、404清理、现有重连/旧响应测试；UUID构造异常后释放提交状态 | passed |
| BT-005 | BacktestReport / EquityChart | null/零平仓解释、连续分页、10页截断提示、重试和现金/持仓悬停 | passed |
| BT-006 | OrdersTradesTables | 默认成交按需订单、标签页保留、精度、空表、首屏/续页重试、旧任务失败隔离、枚举中文映射 | passed |
| BT-007 | 本地门禁 + 独立review | 见下 | passed |
| MySQL | 无SQL/数据库持久化/迁移/事务变动 | 不触发外部门禁 | not-required |
| image | 无依赖版本/镜像打包/部署方式变动 | 不触发外部门禁 | not-required |

## 自动验证

- 基线 `npm --prefix web test -- --run`：25文件/207测试通过。
- 新回归在旧实现先失败：恢复空态、草稿刷新、旧报告保留、曲线截断/重试、表格标签及重试。评审指出的UUID异常以失败测试复现后修复。
- `bash scripts/verify.sh`：退出0；前端221测试及生产构建、文档、Go全量、Race、go vet、性能、容器配置安全检查全部通过。
- 随后仅追加订单原因中文枚举（0–17对应既有领域值）、未知编号回退及测试。对最终实现重新执行 `npm --prefix web run check`：26文件/222测试通过，构建通过；语句93.73%、分支87.65%、函数90.78%、行95.89%。`go test . -run '^TestDocumentation' -count=1`、`git diff --check`通过。后端未改变，无需重跑相同Go门禁。
- Go总覆盖率86.6%；market94.3%、indicator92.5%、strategy94.8%、backtest90.4%，满足各自门槛。
- 性能门禁：全市场229ms、batch_reads=1（本次运行样本，不作服务SLA）。

## 独立评审

子agent backtest_review只读审查架构、遗留清理、简化、BUG及设计符合性。发现P2：请求UUID构造在try外导致异常时永久提交中；已移入try，回归验证可再次提交。增量复核了证券元数据补全、订单结果枚举和文档，最终无阻塞问题。

## 浏览器验收

沿用用户此前对同一工作台浏览器验证的授权，在127.0.0.1:5174隔离前端连接本机8080服务。未改配置、数据库结构或行情。

- 桌面：顶部条件、折叠费用/参数、报告摘要和权益曲线布局正常；一年窗口实际执行中国平安回测，两轮策略（底部倍量回撤、日线B1）均完成。
- 新一轮提交后旧报告保留并提示，成功后切换为日线B1报告；草稿改策略不会篡改旧报告标签。
- 日线B1实际返回5条未成交订单（既有引擎原因14，行情数据无效），中文解释正确；本次不改变引擎/行情问题，零成交不伪造成收益。非空成交显示与金额精度由自动测试验证。
- 切图表再返回：策略、报告、订单标签及5条记录保留；刷新恢复日期/策略和报告原始条件。
- 375px：条件正常换行，指标两列，曲线和辅助信息可见，订单标签可操作，无页面级横向溢出的视觉迹象；完成后关闭设备模式和DevTools。
- 控制台两个404来自既有图表页SHFE:FU.MAIN关联行情，不是回测资源；回测状态/权益/订单请求正常。

本地8080静态产物更新与合并后冒烟待在同日交付记录补记，不影响上述隔离实现验收。
